# Structure decisions and remaining work

8 September 2026. Reconciles the original UX grilling with the accepted prototypes and inspected application code. This is a delivery/status record, not a new product specification. The goal-view slice is implemented in production source; hosted deployment remains separate.

Latest visual decision: the user explicitly selects the All Goals table in `PwuD7m/image.png` and the simpler goal detail in `kzdOv8/image.png`. The expanded goal dashboard in `cE4DCd/image.png` is rejected. The real app now uses the selected composition, retaining its own shell, branding and typography. The user additionally restored the weekly time-budget bar (`TQ5bdP/image.png`) and the dated action/streak/milestone section beneath the main input graph (`80DNdo/image.png`), and chose “Live experiment” for the current test. The timing-edit study is also rejected as a foundation for general coaching changes. This supersedes the later integrated goal composition, not the accepted coaching methodology or truthful-state requirements.

## Already decided

- One All Goals overview. Goal → Plan → Milestones → Actions expresses work ownership, through meaningful visual grouping and time. Plan, actions, milestones and progress are not separate app-level destinations.
- The screenshot-selected All Goals A and earlier Goal C are the visual baseline. All Goals keeps aligned goal/result, activity, input graph and milestone columns. Goal C has the learning journey on the left, one main action canvas on the right and a subordinate outcome projection when supported. The later action-grid dashboard is not the selected C. The GitHub-style activity grid is exclusive to All Goals. The integrated dated action cells/streak lane beneath the goal input graph are a separately approved part of C.
- Multiple milestones can be active, ongoing actions retain identity and learning across milestones, and small goals need no invented milestone. Other current/upcoming/completed work must remain discoverable in the selected context. The later attempt to expose everything through extra default grids and panels is superseded by the screenshots.
- Each milestone leads with its own success criterion. Work completion and outcomes remain separate when they measure different things. Supported conditional projections retain their range; unknown relationships do not manufacture dates.
- One Coach handles conversation and check-ins. Contextual action controls and quick replies use the same records, with an explicit dated report and a correctable save receipt. Check/x/edit/calendar is tied to the selected action and actual state.
- Coach opened from a goal sits beside its plan on desktop, preserving the selected action and timeline. Entering Coach from navigation opens its full workspace. Mobile provides a clear return to the selected work. Both presentations use the same conversation, memory and records; the user's “ok” to the preceding placement question is recorded as acceptance.
- Conversation and Insights are two views within Coach. Insights exposes active experiments and what Adler is learning, linked to the affected goals; selecting an entry reveals its evidence and learning history. These are views of the same authoritative records, not separate coaching systems.
- Proposed changes preview Current → Suggested effects in the existing plan. Acceptance, scheduling, reported use and a review result remain different events. This preview is an accepted requirement, not a newly proposed feature.
- One coherent suggestion affecting several actions is accepted together. Highlight its affected work and actual consequences in the existing view; Edit or Discuss can revise any part before acceptance, with the proposal and affected hypothesis updated consistently. Unrelated suggestions stay separate. Existing authorization still applies.
- Experiments are general coaching changes with personal evidence, specific research, a hypothesis, an adjustment and a review. Time represents when a change was introduced, used or reviewed; a scheduling field is not the experiment's foundational logic. Preserve the actual meaning of different adjustments and disclose the saved rationale in context.
- Meaningful plan changes and reviews form the goal's learning journey. Related experiments open from the affected work and from Coach as the same record.
- Insights groups useful learning questions and scoped findings about the person. Goals are linked context and a filter. Original predictions, personal observations, cited behavioural interpretations, evidence, review timing and corrections remain inspectable. A fact need not be an experiment or insight.
- Insights defaults to current experiments and still-useful findings. Completing a test does not hide a useful finding. Older attempts are available through history; consequential uncertainty, corrections and conflicting evidence stay apparent. This is disclosure, not an evidence-promotion or automatic archive policy.
- The coach chooses useful actions, measurement, cycle and learning design from the person, goal and owned methodology. There is no universal review interval, adherence target or automatic difficulty increase.

These decisions should not be repeatedly re-grilled. Their concrete interaction and difficult cases still need to be resolved or verified below.

## What is still needed

| Area | Current state | Remaining work |
| --- | --- | --- |
| All Goals | Production rows show saved goal/result, compact activity, input line graph and milestone/outlook. The restored time bar uses the same scheduled-commitment calculation as capacity validation and labels the user's budget. | Comprehension checks with real users; retain the corrected grid scope. |
| Goal plan | Production C has saved plan/review chronology, selected action controls, one input graph, dated action/streak strip, milestone context and shared conditional goal outlook. Earlier plans and the old first-action scheduling flow remain accessible. | Deeper concurrent-milestone/dependency/contribution design remains open. The current model cannot calculate milestone finish dates from milestone prose. Preserve the selected composition. |
| Coach | The shared service and conversation already exist. Navigation and the conversation still say Check-in. Prototype conversation is only a local draft. Contextual placement is agreed. | Design/implement the accepted placement, reporting quick replies, typed inline context, actual save/correction receipts and the proposed-change flow in that same conversation. Final primary/utility navigation still needs an explicit composition. |
| Insights and experiments | Durable learning records, revisions, source rationale, controls and a learning journey exist. The current dashboard primarily groups records into current/past. Placement within Coach alongside Conversation and the default of current experiments plus still-useful findings are agreed. | Compose the accepted question-first overview: what is being learned, current test/experience, next review and practical implication. Opening an entry reveals its experiments/history, conflicting evidence and source reasoning. Verify selection and history disclosure while keeping consequential corrections, uncertainty and original scope apparent. |
| Change preview | Existing proposals and durable learning are broader than scheduling. The three timing-edit variants are archived and retired from the active preview; no editing variant or scope default was accepted. Grouped suggestion acceptance remains agreed. | Establish the general change explanation inside the selected goal/Coach views, including non-time adjustments. Connect what changes, why, the expected signal and later evidence without introducing new default panels. Resolve scope and calendar details as subordinate cases. |
| First goal | A short goal-entry form already hands off to conversation. | Verify the complete route to a useful editable first plan, coach-selected tracking and an appropriate initial timeline. Include complex/qualitative goals, insufficient context and unavailable-provider recovery. |
| Tools and settings | Calendar, connections, reminders and coaching/provider settings already have surfaces. | Clarify their placement and contextual entry points. Preserve one coach and actual read/write capability; avoid presenting tools or settings as additional learning systems. |
| Scientific and state integrity | Shared methodology, claim grounding, reviewed advice and durable learning exist. | Apply the accepted second-pass evaluation cases to real flows. Resolve date-derived test-use labels, sparse/single-driver projection applicability, useful action size, delayed outcomes and correction propagation. Define milestone forecast dependencies only where defensible; current projections do not implement a full milestone dependency forecast. |
| Streaks and exceptions | Production counter checks the saved daily commitments and measured targets. Planned rest continues an established count; unknown closed days interrupt it; unknown today preserves the previous count without marking today on plan. Inactive goals display no active count, and counts do not grow outside the saved planning window. | More general period-based adherence and pause/resume policy remain open. This is a display heuristic, not a habit-strength measure or automatic difficulty/optimization rule. |
| Validation and release | This slice has model, service, browser, desktop/mobile and accessibility checks; see [goal-view verification](evaluation/goal-views.md). Vercel was checked: no deployments and no backend environment variable. | User comprehension and hosted health/authentication/persistence checks remain. withadler.com needs an actual persistent backend. |

## Recommended order

1. Continue the remaining structural grilling from the implemented All Goals and goal views. Work through how a general coaching change is understood and inspected in that existing frame, using non-time cases. Do not ask the user to choose among the retired scheduling layouts or reapprove the already settled hierarchy, Coach placement, Insights grouping and grouped acceptance.
2. Walk one complete journey: enter a goal → first useful plan → report experience → inspect a supported proposed change → apply it → report use → review learning. Include a correction and a changed circumstance. Settle remaining milestone/dependency, edit-scope and recovery rules where that journey exposes them.
3. Implement coherent pieces of that agreed journey in the existing shared system. Verify scientific/state meaning alongside the interface rather than treating it as later polish.
4. Validate cross-goal, short-goal, unknown-outcome and mobile cases. Then update actual-app landing captures and verify release. The Today redesign remains deferred.

Current implementation slice: the two selected compositions, restored top/bottom strips and “Live experiment” wording are in the real app. Reports and corrections use shared persistence, calendar controls keep the selected action, and scientific detail uses saved rationale. Existing proposals are reviewed in a disclosure and accepted through the shared service. This does not complete the broader contextual Coach placement, grouped proposal editing, question-first Insights, milestone dependency forecast or Today redesign. The general experiment model remains broader than scheduling.

## Audit references

- Original grilling: G04–G13 (hierarchy, time, reporting and proposals), G16–G19 (action continuity, C, goal history and Insights), G22–G29 (A, progress implications and accepted second pass), G31 (All Goals-only activity), and G33 (contextual Coach placement) in the [archived decision map](https://github.com/jordymarshall/adler/blob/prototype/goal-views-integrated/docs/prototypes/goal-views-integrated/design-map.md). G34–G36 record subsequent accepted decisions; G37 records the editing study; G38 supersedes its foundational use and restores the explicit screenshot composition. Newer entries are in the local working map; the published archive remains at the previous push.
- [Governing principles](product-principles.md), [current method](method/adler-method.md), [target engineering](method/coaching-engineering.md), [accepted second pass](product-coaching-second-pass.md).
- Inspected application: `src/App.tsx`, `src/GoalPlan.tsx`, `src/GoalWorkspace.tsx`, `src/GoalOrganization.tsx`, `src/GoalActivity.tsx`, `src/LiveCoach.tsx`, `src/LearningDashboard.tsx`, `src/Insights.tsx`, `src/Onboarding.tsx`, `shared/projection-model.ts` and `shared/goal-projection.ts`.

The latest slice changes production goal views and fixes the affected persistence/history semantics. It introduces no alternate coaching engine or new behavioural inference. Local verification is recorded separately from hosting and effectiveness claims.
