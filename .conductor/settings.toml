"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm ci"
run_mode = "concurrent"

[scripts.run.dev]
command = "npm run dev -- --port ${CONDUCTOR_PORT:-5173}"
default = true
icon = "play"

[scripts.run.build]
command = "npm run build"
icon = "check"
