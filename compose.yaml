services:
  adler:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: 8080
      ADLER_DATA_DIR: /data
    volumes:
      - adler-data:/data
    stop_grace_period: 25s
volumes:
  adler-data:
